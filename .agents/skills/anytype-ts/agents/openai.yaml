interface:
  display_name: "Anytype Ts"
  short_description: "Repo-specific patterns and workflows for anytype-ts"
  default_prompt: "Use the anytype-ts repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true